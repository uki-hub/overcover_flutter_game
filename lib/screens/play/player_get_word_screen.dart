import 'package:flutter/material.dart';

class PlayerGetWordScreen extends StatelessWidget {
  const PlayerGetWordScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}